import logo from 'logo.svg';
import { Link } from 'react-router-dom';

export const Iro = (props) => {
  const { url, iconPath } = props;

  return (
    <div className='App'>
      <img src={image} alt={alt} />
    </div>
  );
};
