import { About, Projects, Contact, ScrollToTop } from 'components';

export const Home = () => {
  return (
    <div>
      <About />
      <Projects />
      <ScrollToTop />
      <Contact />
    </div>
  );
};

export default Home;
